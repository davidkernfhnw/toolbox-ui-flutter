// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility that Flutter provides. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

// import 'package:flutter_test/flutter_test.dart';
// import 'package:geiger_toolbox/app/services/geigerApi/geigerApi_connector_controller.dart';
// import 'package:geiger_toolbox/app/services/localSorage/local_storage_controller.dart';
//
// import 'package:geiger_toolbox/main.dart';
// import 'package:get/get.dart';

void main() async {
  // await Get.put(GeigerApiConnector()).initGeigerApi();
  // await Get.put(LocalStorageController()).initLocalStorage();
  //
  // testWidgets('Geiger Toolbox App', (WidgetTester tester) async {
  //   // Build our app and trigger a frame.
  //   await tester.pumpWidget(GeigerApp());
  //
  //   // Verify that our counter starts at 0.
  //   expect(find.text('Geiger Toolbox'), findsOneWidget);
  //   // expect(find.text('0'), findsOneWidget);
  //   // expect(find.text('1'), findsNothing);
  //   //
  //   // // Tap the '+' icon and trigger a frame.
  //   // await tester.tap(find.byIcon(Icons.add));
  //   //await tester.pump();
  //
  //   // Verify that our counter has incremented.
  //   // expect(find.text('0'), findsNothing);
  //   // expect(find.text('1'), findsOneWidget);
  // });
}
