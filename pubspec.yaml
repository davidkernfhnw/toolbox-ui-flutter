name: geiger_toolbox
description: A cyber geiger toolbox

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.3
  #progress animations
  percent_indicator: ^3.0.1
  #indicator gauge
  pretty_gauge: ^1.0.0

  #add multiple color gradient to text
  simple_gradient_text: ^1.2.2
  #for expansion of cards
  expandable: ^5.0.1
  #google fonts
  google_fonts: ^2.1.0
  json_annotation: ^4.3.0
  qr_flutter: ^4.0.0
  qr_code_scanner: ^0.5.2
  permission_handler: ^8.2.2

  flutter_form_builder: ^7.0.0
  device_info_plus: 3.2.1

  cloud_replication_package:
    path: ../toolbox-replication/

  #state management, dependency injection
  get: ^4.6.1
  #cache data
  get_storage: ^2.0.3
  charts_flutter: ^0.12.0
  toolbox_indicator_test: ^1.0.5
  geiger_api: ^0.6.3
  flutter_local_notifications: ^9.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^6.0.0
  #runner for generating json
  build_runner: ^2.1.4
  flutter_lints: ^1.0.4
  flutter_native_splash: ^1.3.1


# The following section is specific to Flutter.
flutter:
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
  fonts:
     - family:  GeigerIcon
       fonts:
         - asset: assets/fonts/GeigerIcon.ttf


#Todo
#add to web/index.html
#<script src="https://cdn.jsdelivr.net/npm/jsqr@1.3.1/dist/jsQR.min.js"></script>
